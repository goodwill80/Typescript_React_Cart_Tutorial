import styled from 'styled-components';
import Menu from '@mui/icons-material/Menu';

export const Wrapper = styled.div`
  margin: 40px;
`;

export const StyledButton = styled(Menu)`
  position: fixed;
  z-index: 100;
  right: 20px;
  top: 20px;
`;
